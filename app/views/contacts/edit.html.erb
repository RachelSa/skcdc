<div id="left-col" class="col l4">
  <h5>Admin Account</h5>
  Is <%= @contact.title %> no longer a contact?<br>
  <%= button_to "delete contact", @contact, :method => :delete, data: {confirm: "Are you sure you want to delete this contact?"}, class: "waves-effect waves-light amber lighten-1 white-text btn" %>


</div>

<div id="right-col" class='col l6'>
  <h5>Edit <%= @contact.title %></h5>
    <%= render partial: 'form' %>
</div>
